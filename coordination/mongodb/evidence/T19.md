# T19 운영 전환 증적

## 승인과 전환

- 사용자가 `main` 병합과 실제 운영 서버 전환을 명시적으로 승인했다.
- 최초 전환 커밋 `b8bf80d3a250052f63180e9a85bca04313c7b593`을 배포했다.
- 운영 검증 중 확인한 sanitizer 오탐과 공개 경로 session matcher를 수정한 뒤
  `d16ce9d3382d5b513793c5ba05f3ba945db907a5`까지 배포했다.
- GitHub 저장소 변수 `MONGODB_CUTOVER_APPROVED=true`를 설정했고 자동 배포
  [run 33312145248](https://github.com/Expresso-Organization/expresso/actions/runs/33312145248)이 성공했다.

## 이관과 백업

- import run: `827ca2d2-3735-4f43-a630-99c0503a1861`.
- source schema hash: `c7e44695e3a9d1c25b670d7ba2b0ebc69e96067d1c40c8a63398e0f78971d9c0`.
- `job_source` 5/5, `company` 6/6, `job_posting` 189/189,
  `job_posting_requirement` 12/12이며 verify-only 불일치 0건이다.
- 전환 전 백업과 이관 후 백업을 서버와 로컬에 각각 보존했고 두 복제본의
  `SHA256SUMS` 검증이 통과했다.
- 기존 MySQL 컨테이너·볼륨과 Redis volume을 삭제하지 않았다.

## 실제 운영 검증

- migration `0001`–`0004`를 적용했다.
- Queue prefix를 `expresso-mongo-v1`로 전환했다.
- 가입, 동의, 커리어 기록 저장, 이관 공고 조회, Recipe 생성, 포트폴리오 생성,
  공개 배포를 하나의 운영 시나리오로 검증했다.
- 최종 smoke 결과는 `status=PASS`, imported jobs 189, readiness 200,
  public API 200, public Web 200이다.
- API·Worker·Web은 active이고 MongoDB·Redis·보존 MySQL은 healthy다.

비밀 값과 검증 계정 자격 증명은 기록하지 않았다. 상세 실행 시각, 이미지 digest,
백업 경로와 복구 경계는 `docs/operations/MONGODB_CUTOVER.md`에 기록했다.

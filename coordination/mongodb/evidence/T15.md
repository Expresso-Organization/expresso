# T15 검증 증적

## 구현

- 삭제 요청·취소·내보내기·단계별 purge와 사용자 쓰기 guard를 MongoDB로 전환했다.
- 소유권 명세에 따라 사용자 문서를 단계별로 제거하고 공통 공고 자산은 제외하며, checkpoint와 audit은 재시작 가능하게 유지한다.
- 예약 작업은 `(jobKey, scheduledFor)` 중복 방지, lease 만료, 재시도, outbox 저장을 MongoDB 트랜잭션으로 처리한다.

## 검증

- 29일 보존/30일 purge, 즉시 공개 자산 폐기, 취소, 단계 실패 후 재개, 잔여 사용자 문서 0건을 MongoDB 통합 테스트로 검증했다.
- `pnpm test`: backend 398 통과.
- `node scripts/test-infra.mjs`: 31 files / 170 tests 통과.
- 직접 검토에서 외부 상태 enum 불변, 공통 공고 제외, 재실행 가능성, 예약 중복 방지를 확인하고 승인했다.

{
  "name": "@expresso/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "migrate": "node --env-file-if-exists=../../services/backend/.env --import tsx src/cli.ts",
    "build": "tsc -p tsconfig.build.json && node src/mongo-build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@expresso/contracts": "workspace:*",
    "mongodb": "7.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.11",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}

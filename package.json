{
  "name": "expresso",
  "private": true,
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present build",
    "typecheck": "pnpm --recursive --if-present typecheck",
    "test": "pnpm --recursive --if-present test",
    "build:portfolio-css": "node scripts/build-portfolio-css.mjs",
    "check:portfolio-css": "node scripts/build-portfolio-css.mjs --check",
    "ml:data:test": "node --test scripts/ml-data/*.test.mjs && uv run --with duckdb python scripts/ml-data/profile_lib_test.py && uv run python scripts/ml-data/synthetic_profile_test.py && uv run python scripts/ml-data/synthetic_profile_batch_test.py && uv run python scripts/ml-data/retrieval_baselines_test.py && uv run python scripts/ml-data/ranking_evaluation_test.py",
    "ml:data:collect": "node scripts/ml-data/collect.mjs",
    "ml:data:profile": "uv run --with duckdb python scripts/ml-data/profile.py",
    "ml:eval": "uv run python scripts/ml-data/evaluate_retrieval.py",
    "db:migrate": "pnpm --filter @expresso/database migrate",
    "migrate:jobs-to-mongodb": "node scripts/operations/migrate-mysql-to-mongodb.mjs",
    "infra:up": "docker compose -f infra/compose.yaml up -d --wait mongodb redis && docker compose -f infra/compose.yaml run --rm --no-deps mongodb-init",
    "infra:down": "docker compose -f infra/compose.yaml down",
    "infra:ready": "docker compose -f infra/compose.yaml ps --format json",
    "infra:mongodb:up": "docker compose -f infra/compose.mongodb.yaml up -d --wait mongodb && docker compose -f infra/compose.mongodb.yaml run --rm --no-deps mongodb-init",
    "infra:mongodb:down": "docker compose -f infra/compose.mongodb.yaml down",
    "infra:mongodb:ready": "docker compose -f infra/compose.mongodb.yaml ps --format json",
    "test:infra": "node scripts/test-infra.mjs",
    "dev:backend": "pnpm --filter @expresso/backend dev",
    "dev:worker": "pnpm --filter @expresso/backend dev:worker",
    "dev:web": "pnpm --filter @expresso/web dev"
  },
  "devDependencies": {
    "mongodb": "7.5.0",
    "mysql2": "^3.23.4"
  }
}

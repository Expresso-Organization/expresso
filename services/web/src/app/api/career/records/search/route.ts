import { API_PREFIX,CareerRecordListResponseSchema } from "@expresso/contracts";import { API_BASE_URL } from "@/lib/api/client";import { readAccessToken } from "@/lib/session";
export async function GET(request:Request){const token=await readAccessToken();if(!token)return new Response(null,{status:401});const query=new URL(request.url).searchParams;const upstream=await fetch(`${API_BASE_URL}${API_PREFIX}/career/records?${query}`,{headers:{authorization:`Bearer ${token}`,accept:"application/json"},cache:"no-store"});if(!upstream.ok)return new Response(null,{status:upstream.status});try{return Response.json(CareerRecordListResponseSchema.parse(await upstream.json()))}catch{return new Response(null,{status:502})}}

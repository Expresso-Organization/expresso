/**
 * 00b · 06에 남은 화면 문구.
 *
 * 공고 · 일치도 · 관심 · 집계는 이제 `GET /v1/jobs/postings`에서 옵니다.
 * 여기 남은 것은 데이터가 아니라 정의서가 정한 문구입니다 — 검색창의
 * 예시 문장과, 최근 검색이 아직 쌓이지 않았을 때 보여줄 제안입니다.
 */

export const BROWSE_QUERY_PLACEHOLDER =
  "리모트 되고 데이터 파이프라인 다루는 백엔드, 연봉 8천 이상";

/** 최근 검색이 두 건도 안 될 때 대신 보여주는 제안. */
export const SIMILAR_SEARCHES = [
  "스트리밍 없는 데이터 팀",
  "정산 · 결제 도메인",
  "시리즈 B 이상",
] as const;

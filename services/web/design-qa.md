# 커리어 속성 추가 팝오버 디자인 QA

## 검증 조건

- 기준 이미지: `/var/folders/wy/zrbg0t692zq6smx73mhzsdhr0000gn/T/codex-clipboard-092fe86f-42d4-47f4-87d4-5d36e0db7079.png`
- 구현 화면: `http://127.0.0.1:3200/career/e2e_properties_a86af0b5652a4e9bb9efe8f3344c2f85`
- 구현 캡처: `/tmp/expresso-career-property-qa/implementation-full.png`
- 전체 비교: `/tmp/expresso-career-property-qa/full-comparison.png`
- 집중 비교: `/tmp/expresso-career-property-qa/focused-comparison-510.png`
- 상태: 다크 테마, 커리어 기록 드로워 열림, 속성 추가 팝오버 열림
- 데스크톱 viewport: 브라우저 콘텐츠 영역 약 `1175 × 1027px`
- 모바일 viewport: 요청값 `375 × 812px`, 실제 콘텐츠 영역 `341 × 738px`
- 기준 원본: `1160 × 1537px`. 팝오버 비교 영역 `584 × 930px`을 `320 × 510px`로 정규화했다.
- 구현 비교 영역: `320 × 510px`. 같은 픽셀 크기로 나란히 배치해 비교했다.

## 비교 결과

### 전체 화면

- 전체 화면을 덮던 배경과 속성 관리 좌우 패널이 제거됐다.
- `속성 추가` 버튼에서 팝오버가 열리고 기존 드로워 문맥이 그대로 보인다.
- 팝오버는 Expresso의 배경, 테두리, 전경색, 그림자 토큰을 사용한다.
- 데스크톱에서는 트리거 근처에 열리고, 모바일에서는 좌우 12px 이상을 남겨 화면 안에 들어온다.

### 집중 영역

- 기준 이미지의 상단 아이콘·속성명 입력·유형 제목·단일 열 타입 목록 구조를 재현했다.
- Phosphor 아이콘 세트로 각 속성 유형을 구분했다.
- 관계·수식·롤업은 오른쪽 화살표로 다음 설정 단계가 있음을 표시한다.
- 기준 이미지의 AI 자동 채우기는 현재 Expresso 속성 생성 계약에 연결된 동작이 없어 포함하지 않았다.
- 상태·사람·생성자 같은 미지원 또는 시스템 전용 타입을 숨기고, Expresso가 지원하는 파일과 미디어를 각각 제공한다.

### 필수 충실도 항목

- 글꼴·타이포그래피: Expresso UI 글꼴과 14px 타입 라벨을 유지했고, 입력 placeholder·그룹 제목·유형 라벨의 위계를 분리했다. 줄바꿈과 잘림은 없다.
- 간격·레이아웃: 팝오버 폭 320px, 12px 반경, 30px 타입 행, 단일 열 목록을 적용했다. 데스크톱과 모바일에서 화면 밖으로 넘치지 않는다.
- 색상·토큰: 배경, hover, 테두리, 초점 링, 전경색, 그림자는 모두 기존 `--ex-*` 역할 토큰을 사용한다.
- 이미지·에셋: 이 팝오버에는 래스터 이미지가 없다. 모든 유형 표시는 제품 표준 Phosphor 아이콘 글꼴을 사용해 선명도와 세트 일관성을 유지했다.
- 문구·콘텐츠: 기준의 범용 타입명을 한국어로 유지하고, Expresso가 지원하지 않는 항목과 동작은 노출하지 않았다.

## 상호작용과 접근성

- 속성명 입력 후 기본 타입을 누르면 미리보기·적용 API를 거쳐 생성되고 팝오버가 닫힌다.
- 이름을 비우고 타입을 누르면 타입명이 기본 속성명으로 사용된다.
- 선택·다중 선택은 유효한 빈 선택지 배열로 생성된다.
- 관계·수식·롤업은 같은 팝오버 안에서 필수 설정을 받은 뒤 생성한다.
- 바깥 클릭과 `Escape`로 닫히고, `Escape` 사용 후 트리거로 초점이 돌아간다.
- 키보드로 이름 입력과 타입 버튼을 순서대로 탐색할 수 있다.
- 실제 브라우저에서 텍스트 속성 생성, 팝오버 닫힘, 목록 즉시 반영을 확인했다.
- 새 검증 탭에서 콘솔 오류가 없음을 확인했다.

## 비교 이력

1. 첫 구현은 폭 340px, 타입 행 높이 34px로 표시되어 기준보다 가로 폭이 넓고 한 화면에 보이는 타입 수가 적었다.
2. 폭을 320px로 줄이고 타입 행 높이를 30px로 조정했다.
3. 수정 후 집중 비교에서 입력 행, 유형 제목, 아이콘·라벨 정렬과 목록 밀도가 기준에 가까워졌고 P0/P1/P2 차이가 남지 않았다.

## 남은 P3

- 기준 이미지와 Expresso가 사용하는 글꼴이 달라 글자 폭과 안티앨리어싱이 완전히 같지는 않다. 제품 전체 글꼴 일관성을 위해 Expresso UI 글꼴을 유지했다.

final result: passed

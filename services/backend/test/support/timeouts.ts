/**
 * 격리 데이터베이스 하나를 세우고 거두는 데 주는 시간.
 *
 * 파일마다 데이터베이스를 새로 만들어 마이그레이션을 전부 적용한다. MySQL 은
 * DDL 문장마다 커밋하므로 그 비용이 준비 시간에 그대로 실리고, 묶음 전체를
 * 한꺼번에 돌리면 여러 파일이 같은 서버에 동시에 몰린다.
 */
export const ISOLATED_DATABASE_TIMEOUT_MS = 60_000;

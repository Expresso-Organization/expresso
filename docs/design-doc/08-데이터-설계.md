# 8. 데이터 설계

## 8.1. ERD

데이터베이스는 **PostgreSQL 18.4**, 데이터베이스명은 `expresso`다. 스키마는
`packages/database/migrations/`의 마이그레이션 54개로 정의한다. 현재 테이블은 **75개**다.

> `[그림 8.1] Expresso ERD — `docs/Expresso ERD.dc.html`을 기준으로 그린다. DB이름과
> 테이블이름을 표기한다. 75개를 한 장에 넣으면 읽을 수 없으므로 아래 9개 영역으로 나눠
> 그리고, 영역 간 참조는 화살표로만 잇는다.`

| 그림 | 영역 | 테이블 수 |
| --- | --- | --- |
| 8.1a | 계정 · 권한 | 8 |
| 8.1b | 커리어 기록 | 9 |
| 8.1c | 채용 공고 | 11 |
| 8.1d | 공고 분석 | 2 |
| 8.1e | 포트폴리오 생성 | 19 |
| 8.1f | 포트폴리오 · 편집 | 9 |
| 8.1g | 배포 · 공개 | 4 |
| 8.1h | 분석 · 참여 | 10 |
| 8.1i | 플랫폼 | 3 |

### 핵심 관계

```text
user ──1:N──▶ category ──1:N──▶ record ──N:M──▶ record_link
  │                                │
  │                                └──1:N──▶ skill_evidence ──N:1──▶ skill
  │
  ├──1:N──▶ brew ──1:1──▶ interview_session ──1:N──▶ question ──1:1──▶ answer
  │           │
  │           ├──1:1──▶ recipe ──1:N──▶ recipe_item ──1:N──▶ recipe_evidence_path
  │           │                                                      │
  │           └──1:N──▶ generation_job                               │
  │                        │                              (record를 가리킨다)
  │                        ▼
  ├──1:N──▶ portfolio ──1:N──▶ portfolio_section ──1:N──▶ block
  │             │                                          │
  │             ├──1:N──▶ revision                          └──1:N──▶ generation_sentence_evidence
  │             └──1:N──▶ deployment ──1:N──▶ visit_event
  │
  └──1:N──▶ requirement_coverage ──N:1──▶ job_posting_requirement ──N:1──▶ job_posting
                                                                              │
                                                                        N:1 ──┴──▶ company
```

**요건 충족 판정이 앉는 자리**는 `requirement_coverage`다. 공고 분석 단계에서 외부 LLM이 채운다. `user` × 
`job_posting_requirement`가 복합 기본키이고, `covered_by`가 근거 기록의 배열이다.

### 근거 추적 경로

이 서비스의 핵심 불변 조건은 "공개된 문장은 사용자 증거를 가리킨다"이다. 그 경로가
스키마에 있다.

```text
block (생성된 문장)
  └─▶ generation_sentence_evidence
        └─▶ recipe_evidence_path
              └─▶ record (사용자가 쓴 기록)
```

이 경로가 끊긴 블록은 생성 단계에서 거부된다
(`services/backend/src/modules/generation/validator.ts`).

## 8.2. 논리적 DB설계 (테이블명세서)

8.1의 모든 테이블에 대해 아래 스타일의 테이블 명세서를 작성한다. 아래는 형식 예시 3건과
전체 테이블의 컬럼 요약이다.

### 테이블명(사용자) : USER

| NO | Attribute | Data Type | NN | PK | FK | Description |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `id` | UUID | O | O | | 사용자 식별자 |
| 2 | `email` | CITEXT | O | | | 이메일. 대소문자 구분 없이 유일 |
| 3 | `display_name` | TEXT | O | | | 표시 이름 |
| 4 | `plan_id` | UUID | O | | O | 요금제 참조 (`plan.id`) |
| 5 | `deletion_requested_at` | TIMESTAMPTZ | | | | 삭제 요청 시각. 없으면 정상 계정 |
| 6 | `created_at` | TIMESTAMPTZ | O | | | 생성 시각 |

### 테이블명(경력기록) : RECORD

| NO | Attribute | Data Type | NN | PK | FK | Description |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `id` | UUID | O | O | | 기록 식별자 |
| 2 | `user_id` | UUID | O | | O | 소유자 (`user.id`) |
| 3 | `category_id` | UUID | O | | O | 카테고리 (`category.id`) |
| 4 | `title` | TEXT | O | | | 기록 제목 |
| 5 | `status` | TEXT | O | | | `draft` / `organized` / `verified` |
| 6 | `origin` | TEXT | O | | | `manual` / `ai` / `interview` / `import` |
| 7 | `properties` | JSONB | O | | | 카테고리별 속성. 객체만 허용 |
| 8 | `body_md` | TEXT | O | | | 본문(마크다운) |
| 9 | `period` | DATERANGE | | | | 활동 기간 |

### 테이블명(요구사항충족) : REQUIREMENT_COVERAGE

| NO | Attribute | Data Type | NN | PK | FK | Description |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `user_id` | UUID | O | O | O | 사용자 (`user.id`) |
| 2 | `requirement_id` | UUID | O | O | O | 요구사항 (`job_posting_requirement.id`) |
| 3 | `coverage` | TEXT | O | | | `covered` / `partial` / `missing` |
| 4 | `covered_by` | UUID[] | O | | | 근거가 된 기록 식별자 배열 |
| 5 | `computed_at` | TIMESTAMPTZ | O | | | 판정 시각 |

**4장의 학습 모델을 도입하면 이 테이블에 두 컬럼이 추가된다.**

| NO | Attribute | Data Type | NN | PK | FK | Description |
| --- | --- | --- | --- | --- | --- | --- |
| 6 | `decided_by` | TEXT | O | | | `rule` 또는 `model:{버전}` |
| 7 | `confidence` | NUMERIC(4,3) | | | | 모델 판정 신뢰도. 규칙 판정이면 NULL |

### 테이블명세서(일치도) : MATCH_SCORE

| NO | Attribute | Data Type | NN | PK | FK | Description |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `user_id` | UUID | O | O | O | 사용자 |
| 2 | `job_posting_id` | UUID | O | O | O | 공고 |
| 3 | `total` | INTEGER | O | | | 적합도 0–100 |
| 4 | `axes` | JSONB | O | | | 축별 충족 내역 |
| 5 | `next_action` | TEXT | | | | 다음 행동 제안 |
| 6 | `computed_at` | TIMESTAMPTZ | O | | | 산출 시각 |
| 7 | `decided_by` | TEXT | O | | | 추가 예정 — `rule` 또는 `model:{버전}` |
| 8 | `model_version` | TEXT | | | | 추가 예정 — 판정에 쓴 모델 버전 |

두 테이블 모두에 `decided_by`를 두는 이유는 같다. **어느 쪽이 판정했는지 남기지 않으면
모델 배포 전후의 품질 변화를 추적할 수 없다.** 모델 A와 B는 따로 배포되므로 각각의 자리에
따로 기록해야 한다.

### 전체 테이블 컬럼 요약

아래 75개 전부에 대해 위 형식의 명세서를 작성한다.

### 계정 · 권한

| 테이블 | 주요 컬럼 |
| --- | --- |
| `user` | `id`, `email`, `display_name`, `plan_id`, `deletion_requested_at`, `created_at` |
| `plan` | `id`, `code`, `generation_quota`, `features`, `is_public_listed` |
| `identity_session` | `id`, `user_id`, `token_hash`, `expires_at`, `revoked_at`, `last_seen_at`, `created_at` |
| `identity_oauth_account` | `id`, `user_id`, `provider`, `provider_account_id`, `char_length(provider_account_id)`, `)`, `email`, `linked_at`, `last_login_at` |
| `usage_counter` | `id`, `user_id`, `period_start`, `used`, `resets_at` |
| `consent` | `id`, `user_id`, `scope`, `policy_version`, `granted_at`, `revoked_at` |
| `account_deletion_request` | `id`, `user_id`, `subject_id`, `status`, `requested_at`, `purge_after`, `cancelled_at`, `purged_at`, `cancellation_token_hash` … |
| `account_deletion_event` | `id`, `request_id`, `phase`, `affected_rows`, `occurred_at` |

### 커리어 기록

| 테이블 | 주요 컬럼 |
| --- | --- |
| `category` | `id`, `user_id`, `key`, `is_system`, `property_schema`, `sort_order` |
| `category_view` | `id`, `user_id`, `category_id`, `name`, `view_type`, `filters`, `sorts`, `visible_properties`, `sort_order` … |
| `record` | `id`, `user_id`, `category_id`, `title`, `status`, `origin`, `properties`, `body_md`, `period` |
| `record_link` | `id`, `user_id`, `from_record_id`, `to_record_id`, `relation`, `created_by` |
| `record_usage` | `id`, `user_id`, `record_id`, `block_id`, `quoted_text`, `first_used_at` |
| `skill` | `id`, `user_id`, `name`, `level`, `computed_at`, `demand_score` |
| `skill_evidence` | `id`, `user_id`, `skill_id`, `record_id`, `weight`, `extracted_span` |
| `career_profile` | `user_id`, `target_roles`, `coalesce(array_length(target_roles`, `and`, `)`, `experience_years`, `updated_at` |
| `section_view` | `id`, `user_id`, `visit_event_id`, `portfolio_section_id`, `dwell_ms`, `scroll_depth`, `exited` |

### 채용 공고

| 테이블 | 주요 컬럼 |
| --- | --- |
| `company` | `id`, `name`, `domain`, `industry`, `tone_summary` |
| `job_posting` | `id`, `company_id`, `source`, `external_id`, `title`, `description_raw`, `requirements`, `expires_at`, `dedupe_hash` |
| `job_posting_requirement` | `id`, `job_posting_id`, `order_no`, `label`, `kind`, `source_span`, `extractor_version`, `extracted_at` |
| `job_source` | `id`, `provider`, `token`, `display_name`, `is_active`, `last_run_at`, `last_status`, `last_error`, `last_seen_count` … |
| `requirement` | `id`, `user_id`, `job_analysis_id`, `label`, `kind`, `source_span`, `coverage`, `covered_by` |
| `requirement_coverage` | `user_id`, `requirement_id`, `references`, `coverage`, `covered_by`, `computed_at` |
| `match_score` | `id`, `user_id`, `job_posting_id`, `total`, `axes`, `computed_at` |
| `interest` | `id`, `user_id`, `job_posting_id`, `stage`, `deadline_at`, `memo` |
| `saved_search` | `id`, `user_id`, `query_text`, `filters`, `notify`, `last_run_at` |
| `recent_search` | `id`, `user_id`, `query_text`, `conditions`, `result_count`, `created_at` |
| `company_research_item` | `id`, `user_id`, `company_id`, `kind`, `topic`, `statement`, `source_url`, `published_at`, `captured_at` … |

### 공고 분석

| 테이블 | 주요 컬럼 |
| --- | --- |
| `job_analysis` | `id`, `user_id`, `job_posting_id`, `input_type`, `status`, `attachments`, `analyzed_at` |
| `job_analysis_history` | `job_analysis_id`, `user_id`, `previous_version`, `requirements`, `archived_at` |

### 포트폴리오 생성

| 테이블 | 주요 컬럼 |
| --- | --- |
| `brew` | `id`, `user_id`, `job_analysis_id`, `mode`, `length_preset`, `status`, `deadline_at`, `resumed_at` |
| `brew_job` | `id`, `user_id`, `type`, `status`, `stage`, `attempts`, `input`, `result_id`, `error_code` … |
| `brew_source` | `id`, `user_id`, `brew_id`, `record_id`, `rank`, `selected_by` |
| `interview_session` | `id`, `user_id`, `brew_id`, `status`, `question_count`, `transcript_url` |
| `question` | `id`, `user_id`, `interview_session_id`, `requirement_id`, `replaced_from_id`, `order_no`, `text`, `skipped` |
| `answer` | `id`, `user_id`, `question_id`, `input_type`, `transcript`, `created_record_id` |
| `answer_record_change` | `id`, `user_id`, `answer_id`, `record_id`, `change_type`, `changed_fields`, `jsonb_typeof(changed_fields)`, `and`, `)` … |
| `recipe` | `id`, `user_id`, `brew_id`, `version`, `status`, `completeness`, `generated_at` |
| `recipe_item` | `id`, `user_id`, `recipe_section_id`, `order_no`, `point_text`, `evidence`, `locked`, `edited_by` |
| `recipe_section` | `id`, `user_id`, `recipe_id`, `order_no`, `title`, `purpose`, `target_length` |
| `recipe_revision` | `id`, `user_id`, `recipe_id`, `actor`, `action`, `snapshot`, `diff`, `created_at` |
| `recipe_evidence_path` | `id`, `user_id`, `recipe_id`, `recipe_item_id`, `source_type`, `source_id`, `source_label`, `target_path`, `created_at` |
| `recipe_unused_source` | `id`, `user_id`, `recipe_id`, `record_id`, `reason`, `created_at` |
| `template` | `id`, `code`, `name`, `tone_tags`, `supported_sections`, `plan_required` |
| `layout_spec` | `id`, `user_id`, `portfolio_id`, `batch_id`, `generation_job_id`, `seed_template_id`, `spec`, `prompt_version`, `edited_by` … |
| `generation_job` | `id`, `user_id`, `brew_id`, `recipe_id`, `template_id`, `status`, `usage_charged`, `error_code` |
| `generation_sentence_evidence` | `id`, `user_id`, `generation_job_id`, `block_id`, `recipe_evidence_path_id`, `source_quote`, `created_at` |
| `generation_usage_ledger` | `id`, `user_id`, `generation_job_id`, `usage_counter_id`, `amount`, `reason`, `created_at` |
| `generated_page` | `id`, `user_id`, `portfolio_id`, `html`, `css`, `rationale`, `revision`, `instruction`, `prompt_version` … |

### 포트폴리오 · 편집

| 테이블 | 주요 컬럼 |
| --- | --- |
| `portfolio` | `id`, `user_id`, `brew_id`, `template_id`, `current_deployment_id`, `title`, `status` |
| `portfolio_section` | `id`, `user_id`, `portfolio_id`, `recipe_section_id`, `order_no`, `visible` |
| `block` | `id`, `user_id`, `portfolio_section_id`, `kind`, `content`, `style`, `source_record_id`, `sync_state`, `locked` |
| `portfolio_edit_proposal` | `id`, `user_id`, `portfolio_id`, `target_path`, `block_id`, `operation`, `before_state`, `after_state`, `source_record_id` … |
| `portfolio_snapshot` | `id`, `user_id`, `portfolio_id`, `kind`, `snapshot`, `created_at` |
| `revision` | `id`, `user_id`, `portfolio_id`, `block_id`, `actor`, `before`, `after`, `restore_label` |
| `annotation` | `id`, `user_id`, `portfolio_id`, `date`, `label`, `note` |
| `media_asset` | `id`, `user_id`, `storage_key`, `mime_type`, `width`, `height`, `byte_size`, `created_at` |
| `media_variant` | `id`, `user_id`, `media_asset_id`, `storage_key`, `mime_type`, `width`, `height`, `byte_size`, `created_at` |

### 배포 · 공개

| 테이블 | 주요 컬럼 |
| --- | --- |
| `deployment` | `id`, `user_id`, `portfolio_id`, `version`, `subdomain`, `custom_domain`, `seo_indexable`, `contact_visibility`, `published_at` … |
| `deployment_slug_redirect` | `id`, `user_id`, `portfolio_id`, `old_slug`, `new_slug`, `created_at`, `expires_at` |
| `export_job` | `id`, `user_id`, `portfolio_id`, `deployment_id`, `kind`, `page_format`, `status`, `attempts`, `idempotency_key` … |
| `export_asset` | `id`, `user_id`, `portfolio_id`, `kind`, `file_url`, `page_format`, `download_count` |

### 분석 · 참여

| 테이블 | 주요 컬럼 |
| --- | --- |
| `visit_event` | `id`, `user_id`, `deployment_id`, `session_id`, `referrer`, `org_domain`, `is_owner`, `started_at` |
| `conversion_event` | `id`, `user_id`, `visit_event_id`, `kind`, `target` |
| `analytics_event_receipt` | `event_id`, `user_id`, `deployment_id`, `event_type`, `visitor_hash`, `payload_hash`, `payload_bytes`, `occurred_at`, `received_at` |
| `metric_daily` | `id`, `user_id`, `deployment_id`, `date`, `metric_key`, `value`, `sample_size` |
| `derived_metric` | `id`, `user_id`, `name`, `numerator_key`, `denominator_key` |
| `insight` | `id`, `user_id`, `deployment_id`, `period`, `narrative`, `evidence_metrics`, `suggestions`, `generated_at` |
| `dashboard_view` | `id`, `user_id`, `portfolio_id`, `name`, `period`, `is_default` |
| `widget` | `id`, `user_id`, `dashboard_view_id`, `metric_key`, `derived_metric_id`, `visualization`, `compare_to`, `position` |
| `notification` | `id`, `user_id`, `kind`, `target_url`, `dedupe_key`, `read_at` |
| `notification_preference` | `user_id`, `kind`, `enabled`, `updated_at` |

### 플랫폼

| 테이블 | 주요 컬럼 |
| --- | --- |
| `platform_outbox` | `id`, `topic`, `payload`, `idempotency_key`, `state`, `attempts`, `available_at`, `locked_at`, `published_at` … |
| `scheduled_job_definition` | `job_key`, `interval_seconds`, `next_run_at`, `last_started_at`, `last_finished_at`, `last_status`, `failure_count` |
| `scheduled_job_run` | `id`, `job_key`, `scheduled_for`, `status`, `attempts`, `started_at`, `finished_at`, `last_error`, `result` … |

## 8.3. 로그파일 설계

로그는 **JSON 한 줄에 한 사건**으로 남긴다(`pino`). 사람이 읽는 형식이 아니라 기계가
거르는 형식이다. 장애가 났을 때 필요한 것은 예쁜 문장이 아니라 `requestId`로 한 요청의
전 구간을 모으는 능력이다.

### 파일명

| 프로세스 | 파일명 | 회전 |
| --- | --- | --- |
| API 서버 | `expresso-api.log` | 일 단위 · 14일 보관 |
| Worker | `expresso-worker.log` | 일 단위 · 14일 보관 |
| AI학습서버 | `expresso-training.log` | 일 단위 · 30일 보관 |

학습 로그를 더 오래 두는 이유는, 모델 성능 문제가 배포 후 몇 주 뒤에 드러나기 때문이다.

### 레벨

| 레벨 | 형식 | 설명 | 비고 |
| --- | --- | --- | --- |
| 10 | trace | 개발 중 상세 추적 | 운영에서 사용하지 않음 |
| 20 | debug | 분기·중간 상태 | 운영에서 사용하지 않음 |
| 30 | info | 요청 처리 · 작업 시작/완료 등 정상 흐름 | 운영 기본값 |
| 40 | warn | 재시도 가능한 실패 · 폴백 발동 | |
| 50 | error | 요청 실패 · 작업 실패 | |
| 60 | fatal | 프로세스를 계속할 수 없는 오류 | |

운영 기본 레벨은 `info`이고, `LOG_LEVEL` 환경변수로 바꾼다.

### 공통 필드

| 필드 | 설명 |
| --- | --- |
| `level` | 위 레벨 번호 |
| `time` | 밀리초 단위 epoch |
| `requestId` | 요청 식별자. 응답 헤더 `x-request-id`와 같다 |
| `req.method` | HTTP 메서드 |
| `req.url` | 경로. **쿼리스트링은 잘라낸다** |
| `req.host` | 호스트 |
| `msg` | 사건 이름 |

### 남기지 않는 것

`services/backend/src/platform/observability.ts`가 아래 경로를 `[REDACTED]`로 치환한다.

```text
req.headers.authorization      request.headers.authorization
req.headers.cookie             request.headers.cookie
req.headers['x-api-key']       body.password
body.token                     body.accessToken
body.refreshToken              databaseUrl
redisUrl
```

쿼리스트링을 통째로 잘라내는 이유는, 검색어에 개인정보가 들어가기 때문이다. 무엇을
가려야 할지 하나씩 정하는 것보다 통째로 버리는 쪽이 안전하다.

오류 객체도 그대로 남기지 않는다. `safeErrorSummary()`가 `name` · `code` · `statusCode`만
추린다. LLM 프로바이더가 돌려준 거절 사유처럼 사용자 입력이 섞인 문장은 이 필터를 지난다.

### Log 예시

```text
{"level":30,"time":1786665012345,"requestId":"req_0f8a2c91-...","req":{"method":"POST","url":"/v1/generation-jobs","host":"api.expresso.dev"},"msg":"incoming request"}
{"level":30,"time":1786665012388,"requestId":"req_0f8a2c91-...","jobId":"d5e7a2b9-...","msg":"generation job queued"}
{"level":30,"time":1786665071204,"requestId":"req_0f8a2c91-...","jobId":"d5e7a2b9-...","durationMs":58816,"tokensIn":18442,"tokensOut":6103,"msg":"generation completed"}
{"level":40,"time":1786665130017,"requestId":"req_3b71de4a-...","msg":"training server unreachable, falling back to rule-based ranking"}
{"level":50,"time":1786665188902,"requestId":"req_91cf07b2-...","error":{"name":"GenerationValidationError","statusCode":422},"msg":"request failed"}
{"level":50,"time":1786665201455,"requestId":"req_44ab9e30-...","error":{"name":"Error","code":"ECONNREFUSED","statusCode":503},"msg":"request failed"}
```

### AI 호출 로그

LLM 호출은 비용이 붙으므로 따로 기록한다(`generation_usage_ledger` 테이블 + 로그).

| 필드 | 설명 |
| --- | --- |
| `contract` | 호출 계약 이름 (`job_analysis`, `recipe_draft`, …) |
| `provider` | `claude-code` / `codex` / `fixture` / `off` |
| `model` | 실제 모델 이름 |
| `tokensIn` · `tokensOut` | 토큰 수 |
| `durationMs` | 소요 시간 |
| `costUsd` | 추정 비용 |

## 8.4. 오류코드 및 오류파일 설계

### 서비스 오류코드

API가 답하는 오류 코드는 `packages/contracts/src/common.ts`의 `ErrorCodeSchema`에
정의된 10종이다. HTTP 상태와 1:1로 대응한다.

| 코드 번호 | 코드 | 설명 | 비고 |
| --- | --- | --- | --- |
| 200 | — | 정상 처리(성공) | |
| 400 | `VALIDATION_ERROR` | 요청 스키마 검증 실패 | |
| 401 | `AUTH_REQUIRED` | 인증이 필요하거나 세션이 만료됨 | |
| 403 | `FORBIDDEN` | 인증은 되었으나 권한이 없음 | |
| 404 | `NOT_FOUND` | 리소스가 없음 | |
| 409 | `CONFLICT` | 현재 상태와 충돌하는 요청 | |
| 412 | `PRECONDITION_FAILED` | `If-Match` 버전이 낡음 | 낙관적 잠금 |
| 422 | `UNPROCESSABLE` | 요청은 읽었으나 그대로는 처리할 수 없음 | 근거 검증 실패 등 |
| 429 | `RATE_LIMITED` | 사용량 한도 초과 | |
| 503 | `DEPENDENCY_UNAVAILABLE` | DB · Redis · LLM 등 의존 서비스 장애 | |
| 500 | `INTERNAL_ERROR` | 그 밖의 서버 오류 | |

401과 403을 나누는 이유는 클라이언트의 다음 행동이 다르기 때문이다. 401은 다시 로그인,
403은 다시 로그인해도 소용없다.

### 도메인 오류

같은 `UNPROCESSABLE`이라도 원인이 다르다. `details`에 도메인 코드를 넣는다.

| 도메인 코드 | HTTP | 설명 |
| --- | --- | --- |
| `GEN_UNGROUNDED_NUMBER` | 422 | 생성 문장에 근거 없는 수치가 있다 |
| `GEN_EVIDENCE_OUT_OF_RECIPE` | 422 | 레시피 밖의 근거를 인용했다 |
| `GEN_EXCLUSION_VIOLATED` | 422 | 레시피의 제외 조건을 어겼다 |
| `GEN_LOCKED_BLOCK_CHANGED` | 422 | 잠긴 블록을 바꾸려 했다 |
| `GEN_METRIC_WITHOUT_RECORD` | 422 | 수치 블록이 경력 기록을 인용하지 않았다 |
| `QUOTA_EXCEEDED` | 429 | 생성 횟수 한도 초과 |
| `PLAN_REQUIRED` | 403 | 상위 플랜에서만 쓸 수 있는 기능 |
| `SLUG_TAKEN` | 409 | 이미 쓰이는 공개 주소 |
| `AI_PROVIDER_UNAVAILABLE` | 503 | LLM 프로바이더 응답 없음 |

### AI학습서버 오류코드

| 코드 번호 | 설명 | 비고 |
| --- | --- | --- |
| 200 | 정상 처리(성공) | |
| 000 | 정의되지 않은 에러코드 | |
| 401 | 데이터세트 생성 오류 | |
| 406 | 교사 라벨링 오류 | Batch API 실패 |
| 402 | 원천 데이터 부족 오류 | 최소 행 수 미달 |
| 403 | 라벨러 불일치 오류 | 검수자 2인 합의 실패 |
| 404 | 특징 추출 오류 | |
| 405 | 임베딩 모델 로드 오류 | |
| 411 | 모델 학습 오류 | |
| 412 | 하이퍼파라미터 탐색 오류 | |
| 413 | 학습 중단 오류 | |
| 421 | 성능 검증 오류 | |
| 422 | 배포 승인 기준 미달 | 9.2 기준 불충족 |
| 431 | 모델 배포 오류 | |
| 432 | 모델 버전 관리 오류 | |
| 433 | 모델 롤백 오류 | |
| 434 | 모델 삭제 오류 | |
| 441 | 판정 오류 (모델 A · B) | |
| 442 | 특징 명세 불일치 오류 | 학습·서빙 특징 순서 불일치 |
| 444 | 교사 신뢰도 미달 오류 | κ < 0.6 상태로 학습 요청 |
| 445 | 학습 분포 밖 입력 | 기록 3건 미만 프로필 — 규칙 폴백 |
| 443 | 활성 모델 없음 | 규칙 폴백으로 처리 |
| 451 | 장애기록 반환 오류 | |

442는 조용히 틀린 답을 내는 유일한 경우라, 서빙 시작 시 `feature_spec.json`의 해시를
검증해 미리 잡는다.

### 오류파일

오류만 따로 모은 파일을 남긴다. 운영 중 `error` 이상만 골라 보기 위해서다.

| 항목 | 값 |
| --- | --- |
| 파일명 | `expresso-error.log` |
| 대상 레벨 | 50(error) 이상 |
| 형식 | 8.3과 같은 JSON 한 줄 |
| 회전 | 일 단위 · 90일 보관 |

일반 로그(14일)보다 오래 두는 이유는, 드물게 나는 오류의 재발 주기가 2주보다 길기
때문이다.
